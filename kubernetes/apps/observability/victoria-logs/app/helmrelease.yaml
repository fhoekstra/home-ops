---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app victoria-logs
spec:
  chartRef:
    kind: OCIRepository
    name: victoria-logs
  interval: 1h
  dependsOn:
    - name: rook-ceph-cluster
      namespace: rook-ceph
  values:
    fullnameOverride: *app
    global:
      image:
        registry: quay.io
    server:
      retentionMaxDiskUsagePercent: 80
      retentionPeriod: "1y" # If this is not set, logs older than 7d will be removed, regardless of disk space usage. See: https://docs.victoriametrics.com/victorialogs/#retention-by-disk-space-usage
      persistentVolume:
        enabled: true
        # existingClaim: *app
        storageClassName: "ceph-block"
        size: "20Gi"
      route:
        enabled: true
        hostnames:
          - "vlogs.${CLUSTER_DOMAIN}"
        parentRefs:
          - name: envoy-internal
            namespace: network
        extraRules:
          - filters:
              - type: RequestRedirect
                requestRedirect:
                  path:
                    type: ReplaceFullPath
                    replaceFullPath: /select/vmui/
                  statusCode: 302
            matches:
              - path:
                  type: Exact
                  value: /
      serviceMonitor:
        enabled: true
      resources:
        limits:
          memory: 512Mi
        requests:
          cpu: 30m
          memory: 512Mi
    dashboards:
      enabled: true
      grafanaOperator:
        enabled: true
